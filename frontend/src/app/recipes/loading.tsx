import { NotebookSkelton } from "@/shared/components/Notebook";

/** Renders the notebook skeleton while the recipes route is loading. */
export default function Loading() {
   return <NotebookSkelton />
}