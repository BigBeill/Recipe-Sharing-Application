import LoginPage from "@/features/auth/components/LoginPage";

/** Renders the client-managed login flow without waiting for a server session check. */
export default async function Login() {
   return <LoginPage />
}