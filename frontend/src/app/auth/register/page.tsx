import RegisterPage from "@/features/auth/components/RegisterPage";

/** Renders the client-managed registration flow without waiting for a server session check. */
export default async function register() {
   return <RegisterPage />
}